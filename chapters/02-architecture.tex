\chapter{产品总体设计}

\section{总体架构}

系统采用\textbf{浏览器双端、统一API、模块化业务服务和本地数据层}的分层结构。游客端与管理端共享Vue 3、Vue I18n和统一消息目录，但通过\textbf{路由和角色权限}隔离；Node.js服务统一处理鉴权、问答、语音、路线、数字人、知识管理和分析请求；SQLite保存事务型业务数据，JSON知识库保存可重建的景点实体与分块。总体关系如图\ref{fig:overall-architecture}所示。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.98\textwidth]{system-architecture.pdf}
  \caption{数智游踪系统总体架构图}\label{fig:overall-architecture}
\end{figure}

\subsection{表现与交互层}

游客端围绕\textbf{“看、问、听、游、评”}组织：数字人画面提供视觉主体，文本框和语音按钮承载输入，景点卡片与路线面板承载主动探索，来源和时延信息增强可信感。管理端围绕\textbf{“建、配、看、改”}组织：构建知识、配置数字人、观察运营指标、修正错误问答。顶部语言入口在两端共享，切换后\textbf{导航、标签、表单、提示、数据状态与数字人配置文案}同时更新；移动端宽度下工具区切换为单列，保证景区现场可用。

\subsection{业务与智能服务层}

业务层以\textbf{服务模块}封装能力，入口位于\filepath{server/src/app.mjs}。其中问答服务协调FAQ缓存、护栏、混合检索、大模型和引用；语音服务组织\textbf{ASR--RAG--TTS}；数字人服务保存启用配置并管理形象、音色和回答状态映射，前端调度器依据\filepath{engine}选择\textbf{讯飞或Live2D运行时}；路线服务按偏好评分并生成节点解释；运营服务融合会话、反馈和线下消费数据。

\subsection{数据与外部能力层}

本地层包含\textbf{12张SQLite业务表}，知识快照包含\textbf{7份资料、22个RAG景点实体、20个导览分节和107个知识分块}，另有20条检索测试用例和50条问答测试用例。外部能力以\textbf{适配器}接入OpenAI兼容大模型、ASR/TTS和讯飞交互式数字人。讯飞Web SDK以应用凭证、形象ID和渲染容器启动视频流\upcite{iflytekWebSdk}，项目在此基础上封装\filepath{useXfAvatar.ts}，通过\filepath{writeText}下发答案及情绪参数；本地侧内置\textbf{Live2D Cubism模型与Web Audio口型驱动}，形成不依赖讯飞形象流的备用渲染路径。

\section{整体技术路线}

整体技术路线由需求与资料输入开始，依次完成\textbf{知识工程、智能编排、多模态表达、双端产品化、运营闭环和工程验证}。每一阶段均对应当前项目中的数据产物、服务模块、界面或自动化脚本，形成从赛题要求到\textbf{可部署系统}的纵向实现链路，如图\ref{fig:technical-route}所示。

\begin{figure}[H]
  \centering
  \includegraphics[height=0.70\textheight,keepaspectratio]{technical-route.pdf}
  \caption{数智游踪整体技术路线图}\label{fig:technical-route}
\end{figure}

\newpage
\section{技术选型}

\begin{table}[H]
  \centering
  \caption{系统技术选型与理由表}\label{tab:tech-stack}
  \begin{tabularx}{\textwidth}{p{0.16\textwidth}p{0.25\textwidth}X}
    \toprule
    层次 & 技术 & 选型理由 \\
    \midrule
    Web前端 & Vue 3、TypeScript、Vite、Vue I18n、Element Plus、ECharts & 组件化开发、类型约束、五语言运行时切换和成熟的数据可视化能力 \\
    数字人 & 讯飞交互式数字人SDK、WebRTC、Live2D Cubism、Web Audio & 云端流式形象与本地画布模型共用播报接口，形象与音色可由后台配置 \\
    服务端 & Node.js原生HTTP、ES Modules & 轻量、启动快；服务边界清晰，便于演示部署和自动化测试 \\
    智能问答 & 本地RAG、OpenAI兼容LLM适配器 & 知识可更新、来源可追溯；模型提供者可替换，离线可降级 \\
    数据层 & SQLite、JSON知识库、XLSX导入 & 零配置、单机部署方便；结构化业务数据与可重建知识数据分离 \\
    工程保障 & OpenAPI、Bearer Token、scrypt、日志与备份脚本 & 提升接口一致性、访问控制、凭证安全与故障恢复能力 \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{选型口径与景区约束}

本项目的技术选型以\textbf{“先保证景区核心导览可用，再增强沉浸表达”}为顺序。景区现场与普通互联网产品不同：山地和园区存在弱网、遮挡和定位漂移，游客设备的GPU、麦克风权限和浏览器能力差异明显，旺季并发又会放大云端API费用。因此本节将\textbf{部署难度、本地化能力、云端依赖、实时性、硬件要求、API/服务成本、并发成本、网络依赖、跨平台能力、景区实际部署成本、维护成本和用户体验}作为统一比较维度。表中第一行均为当前项目采用方案；其余方案不是被简单否定，而是说明在什么条件下更合适，以及为什么没有成为本项目的主路径。

\begin{landscape}
\begin{table}[H]
  \centering
  \caption{数字人方案横向比较（本项目选型置前）}\label{tab:compare-digital-human}
  \small
  \renewcommand{\arraystretch}{1.18}
  \begin{tabularx}{\linewidth}{p{0.13\linewidth}p{0.16\linewidth}p{0.16\linewidth}p{0.14\linewidth}p{0.14\linewidth}X}
    \toprule
    方案 & 部署与本地化 & 云端依赖与成本 & 实时性与硬件 & 跨端/维护 & 景区适配判断 \\
    \midrule
    \textbf{讯飞交互式数字人 + Live2D（采用）} & 云端SDK快速接入；Live2D模型、Cubism与音频口型可随Web/桌面包本地部署 & 云端形象按账号、形象权限和调用额度计费；Live2D不产生视频流API费用 & 云端WebRTC表现力高；本地路径主要依赖WebGL、音频分析，不要求独立GPU & Web/桌面共用调度契约；游客端按WebView、原生和小程序能力分层，配置替换集中 & 以在线效果承载演示，以本地形象和文本兜底，平衡体验、网络和现场成本 \\
    MuseTalk & 可自建推理服务，模型和推理链路可控；需准备模型权重、推理服务和视频/音频管线 & 本地无按次API费，但需要GPU服务器、电费和模型运维；高并发需扩容推理副本 & 通常需要较强GPU与显存才能维持可接受帧率；弱设备端不适合直接推理 & 服务端封装复杂，移动端通常仍需远端推理；模型、驱动和版本维护负担较大 & 适合有GPU集群和高质量视频生产需求的场景，不作为本项目景区边缘终端首选 \\
    Live2D（单独采用） & 资源随前端静态部署，素材和运行时可版本化；本地化程度高 & 无云端数字人调用费，但音频仍可能依赖TTS服务；模型素材需授权和持续维护 & WebGL与音频振幅驱动实时性好，普通办公机和手机可运行 & 跨Web/桌面较成熟；小程序和原生需分别适配渲染容器与音频权限 & 是本项目的低成本备用引擎，但单独使用时沉浸式视频表现不如云端形象 \\
    DUIX & 依赖其运行环境、SDK或服务形态，具体本地部署条件需按授权版本确认 & 若采用云端服务需承担账号与调用成本；私有化则增加部署与授权成本 & 能力与实时性取决于SDK版本、终端和网络；不能假设所有终端一致 & 厂商适配和版本升级需跟随生态；跨端边界需逐端验收 & 可作为供应商备选，但当前代码未接入，不能写成已实现主路径 \\
    讯飞云端数字人（单一云端） & 接入路径清晰，主要工作集中在凭证、WebRTC和容器管理 & 形象/音色/并发/网络均受服务额度和账号影响；旺季成本随调用量线性增长 & 在线首帧和口型表现较好，但弱网、额度耗尽或域名限制会直接影响体验 & Web端较顺；小程序个人主体、原生WebView和桌面离线环境需要额外适配 & 适合作为主引擎，但必须保留Live2D/文本兜底才满足景区连续服务要求 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{TTS方案横向比较（本项目选型置前）}\label{tab:compare-tts}
  \small
  \renewcommand{\arraystretch}{1.18}
  \begin{tabularx}{\linewidth}{p{0.16\linewidth}p{0.17\linewidth}p{0.17\linewidth}p{0.15\linewidth}p{0.15\linewidth}X}
    \toprule
    方案 & 本地化/部署 & 云端依赖与成本 & 实时性与音质 & 跨平台与维护 & 选型结论 \\
    \midrule
    \textbf{讯飞TTS（采用）} & 通过Node适配器统一调用，密钥只在服务端；可按句分段并保留音频回退 & 按字符或调用量计费，服务端可集中限流；不把密钥暴露给移动端 & 普通话、情感和音色覆盖较完整；分段合成可缩短首段等待 & Web、桌面和游客端均走同一API，音色由数字人配置下发 & 与讯飞数字人、ASR和现有凭证体系一致，适合当前工程快速闭环 \\
    阿里云TTS & 云端接入文档成熟，也可在后端封装；本地离线需另建引擎 & 按量计费，跨地域和高并发需关注额度、网络与限流 & 音质和语言覆盖较好，首包受网络影响 & SDK与签名维护独立于讯飞体系，替换需维护另一套适配器 & 可作为供应商冗余，当前没有接入代码，不能计入已实现能力 \\
    百度TTS & 云端API接入，离线能力取决于另行部署的本地包 & 按调用量计费；高峰期需做并发与失败重试 & 普通话体验稳定，情感表达和音色要按账号能力核验 & 跨端仍建议由Node代理，增加凭证、监控和版本维护面 & 适合成本或音色策略变化时切换，不作为当前默认 \\
    豆包TTS & 以云端模型服务为主，私有化和端侧部署需看产品授权 & API费用与模型版本策略需持续关注；多景区并发需预算 & 音色和表现力有吸引力，但真实景区网络条件决定首包体验 & 接入方式与现有讯飞不同，移动端不能直接持有密钥 & 可作为未来音色扩展，当前不承担主链路 \\
    浏览器原生TTS & 端侧零部署、零API成本；依赖浏览器/系统语音包 & 无云端费用，但不同系统声音质量和语言包差异大 & 触发快但自然度、停顿和情感不可控，浏览器权限策略影响体验 & Web可用，桌面和移动浏览器差异明显；问题难以统一复现 & 作为最终文本/语音降级入口，而非景区品牌音色主方案 \\
    \bottomrule
  \end{tabularx}
\end{table}
\end{landscape}

数字人和TTS的组合不是简单叠加。讯飞云端形象负责“看起来像在讲解”，讯飞TTS负责“声音可控且能被其他端复用”，Live2D与浏览器原生TTS负责在网络、额度或终端能力不足时保住\textbf{文本—音频—口型}的最小闭环。这样做的工程代价是需要维护音频队列、打断和状态映射，但换来的是景区现场可以明确解释当前引擎与降级阶段，而不是出现无反馈的黑屏或无声。

\begin{landscape}
\begin{table}[H]
  \centering
  \caption{ASR方案横向比较（本项目选型置前）}\label{tab:compare-asr}
  \small
  \renewcommand{\arraystretch}{1.18}
  \begin{tabularx}{\linewidth}{p{0.16\linewidth}p{0.18\linewidth}p{0.17\linewidth}p{0.15\linewidth}p{0.15\linewidth}X}
    \toprule
    方案 & 部署方式 & 网络/API成本 & 实时性与识别边界 & 跨端/维护 & 选型结论 \\
    \midrule
    \textbf{讯飞ASR（采用）} & Node服务端签名并接收移动端上传，服务端统一做格式校验和转写 & 按时长/调用量计费；移动端不保存密钥，便于统一限流与审计 & 流式IAT适合口语问答；项目已验证MP3与8/16kHz单声道PCM/WAV链路 & Web录音、uni.uploadFile、桌面端统一汇入同一服务；供应商配置集中 & 与现有讯飞TTS/数字人凭证和语言策略一致，当前闭环成本最低 \\
    阿里云ASR & 云端服务端接入，需维护另一套签名与错误码 & 按时长计费；跨地域网络和并发额度需要单独治理 & 普通话及多语种能力较完整，真实景区噪声仍需评测 & 可由Node适配，但移动端无需感知供应商；维护两套云服务 & 适合作为后续冗余，不是当前实现 \\
    Paraformer & 可本地部署，模型与推理完全可控；需准备模型、运行时和端侧/服务端资源 & 本地无按次API费，但GPU/CPU、模型更新和运维成本前置 & 离线和弱网优势明显；实时性取决于设备算力和模型大小 & 在服务器上可封装，直接在小程序/手机端运行需评估包体与算力 & 适合未来建设离线语音包；当前以云ASR保证多端一致性 \\
    腾讯云ASR & 云端接入与腾讯地图生态相邻，但仍需独立签名、额度和监控 & 按量计费；供应商切换不会自动共享讯飞额度 & 实时能力可满足通用问答，景区噪声与方言仍需实测 & 适配器可替换，但会增加供应商矩阵与故障分支 & 可作为腾讯生态部署的备选，当前不引入额外依赖 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{地图与定位方案横向比较（本项目选型置前）}\label{tab:compare-map}
  \small
  \renewcommand{\arraystretch}{1.18}
  \begin{tabularx}{\linewidth}{p{0.16\linewidth}p{0.18\linewidth}p{0.17\linewidth}p{0.15\linewidth}p{0.15\linewidth}X}
    \toprule
    方案 & 本地化与网络 & 服务/API成本 & 终端与实时性 & 维护与数据边界 & 选型结论 \\
    \midrule
    \textbf{腾讯地图 + Leaflet/手绘图 + 原生定位（采用）} & Web在线优先腾讯地图 JS GL；离线/Key失败切换Leaflet与本地手绘图；游客端使用uni原生地图与手绘图模式 & 腾讯地图Key和算路/POI按服务策略产生费用；Leaflet底图与本地资源可控；定位由系统能力提供 & 在线底图提供地理语义；本地手绘图保证景区点位与路线；GPS/\texttt{uni.getLocation}和浏览器Geolocation分别适配终端 & 统一处理GCJ-02、WGS84、手绘图旋转、点位和折线；需维护版权归属与坐标审计 & 不是单一地图供应商，而是在线标准能力与景区离线资产互补，最符合现场连续导览 \\
    高德地图 & 在线SDK/Key为主，离线能力需额外购买或缓存；本地手绘图仍要自行维护 & Key、WebService、算路和并发策略独立；高峰期需预算 & 国内地理覆盖和导航体验成熟；不同终端SDK仍需逐端适配 & 坐标、授权、SDK版本和多端包体需统一治理 & 可作为替代在线底图，但无法替代本项目已有手绘资产与多端原生适配 \\
    腾讯地图（单一在线） & 标准底图和定位依赖网络与Key；SDK失败会影响地图主体 & 在线服务按Key、配额和接口计费；弱网时体验不稳定 & Web/微信/原生有官方组件，实时性取决于网络 & 供应商单点故障明显，需要另备本地资产 & 适合作为在线主底图，不能单独承担景区核心导览 \\
    GPS/原生定位能力 & 通过浏览器Geolocation或uni.getLocation获得当前位置，不提供底图和景点语义 & 无地图底图API费用，但需要系统权限、定位耗电和隐私合规 & 在终端允许权限且有卫星/网络条件时可快速定位；室内、山谷和遮挡会漂移 & 需处理权限拒绝、坐标系与最近点位匹配；不能单独绘制路线 & 作为位置输入层，与地图渲染和最近景点匹配结合，不能替代地图服务 \\
    \bottomrule
  \end{tabularx}
\end{table}
\end{landscape}

Web端的真实实现是\textbf{Leaflet先创建地图容器，在线且Key有效时异步加载腾讯地图并隐藏本地瓦片层；offline事件、Key缺失、SDK异常或初始化失败时恢复Leaflet层}。游客端则把同一套点位、路线和坐标数据映射到uni原生\texttt{<map>}与本地手绘图两种渲染模式，并在手绘模式下通过GCJ-02坐标与图块边界计算最近点位。两条路径共同满足“地图服务不可用时仍能看点位、路线和讲解”的目标，但不将Web的Leaflet实现错误地扩写为所有移动端都使用Leaflet。

\subsection{选型结论：为什么采用组合而不是单点最优}

综合比较后，项目采用“讯飞数字人主引擎 + Live2D本地形象、讯飞TTS/ASR服务端适配、腾讯地图在线能力 + Leaflet/手绘图 + 原生定位”的组合，原因有三：

\begin{enumerate}
  \item \textbf{把不可控因素隔离在适配器之后。}云端额度、网络、SDK和终端差异只影响对应适配器，业务页面继续使用标准化答案、音频、点位和路线字段。
  \item \textbf{把景区资产沉淀为本地可重建数据。}22个景点实体、路线锚点、图块边界、手绘素材和知识分块随版本发布，在线服务异常时仍可以解释“在哪里、看什么、怎么走”。
  \item \textbf{把高成本能力放在增强层。}云端视频、TTS、ASR和标准底图提升体验，但核心问答、引用、路线、文本和手绘导览不依赖单一API持续付费，从而控制旺季并发成本和后续维护成本。
\end{enumerate}

结论不是“所有备选方案都不适用”，而是\textbf{当前项目规模、景区部署条件和已有工程资产共同决定了这一组合的成本/体验平衡}。当未来需要大规模离线语音或GPU集群时，Paraformer、MuseTalk等方案仍可沿现有适配器边界接入；当需要更多云端音色或区域化地图服务时，也可以替换供应商而不改写游客端业务流程。

\section{关键设计原则}

总体设计以\textbf{“事实可信、链路可控、能力可替换、现场可交付”}为约束，不以单一模型或单一数字人厂商作为系统成立的前提。各原则同时落实到接口字段、运行状态和管理动作中：

\begin{enumerate}
  \item \textbf{知识有界与证据优先：}模型只解释检索到的景区资料，回答同步返回来源、质量标签和缺证据状态。
  \item \textbf{链路可观测与问题可追溯：}响应记录检索、LLM、ASR、TTS及总耗时，会话、反馈和人工标注能够反查到原始问题。
  \item \textbf{体验渐进增强：}数字人和云语音作为增强能力，异常时自动保留文本问答、本地检索与路线推荐，避免现场服务中断。
  \item \textbf{配置与能力解耦：}语言、形象、音色、渲染引擎、模型提供者、知识数据和路线模板均通过配置或适配器接入，降低替换成本。
  \item \textbf{国际化全局一致：}框架文案和景区业务文案进入同一Vue I18n实例，语言选择写入用户状态并恢复，避免页面局部切换。
  \item \textbf{管理形成质量闭环：}低满意或错误回答可转为知识补充草稿，FAQ修订后进入高优先级通道，形成“发现—修正—验证”闭环。
  \item \textbf{实现、测试与文档一致：}所有指标均对应当前工程脚本、接口和数据产物，不将规划功能表述为既有成果。
\end{enumerate}

\section{功能域划分}

系统按职责划分为六个功能域：\textbf{游客服务域}组织文本、语音、景点讲解与反馈入口；\textbf{知识问答域}负责护栏、FAQ、混合检索、生成与引用；\textbf{数字人域}管理双引擎形象、音色、情绪状态和播报控制；\textbf{路线域}完成偏好采集、模板评分、自适应调整与节点解释；\textbf{运营分析域}汇聚会话、满意度、游客行为和质量指标；\textbf{平台管理域}提供身份、权限、配置、日志与健康检查。\textbf{国际化能力作为横切层}覆盖上述全部功能域，而不是独立于业务页面之外的翻译壳层。

域之间只通过明确的\textbf{服务函数、事件状态和API合同}交互：游客页面不直接访问数据库，数字人只消费标准化答案与表达状态，运营分析只读取脱敏后的业务结构，知识重建不改写历史会话。该边界既保证当前单机同源部署简洁，也为后续替换模型、数字人厂商或前端终端保留\textbf{稳定接口}。
